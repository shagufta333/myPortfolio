import React from 'react'

export const Footer = () => {
    return (
        <footer>
            <p id="footer"> Made with React {React.version}</p>
        </footer>
    );
};
export default Footer;